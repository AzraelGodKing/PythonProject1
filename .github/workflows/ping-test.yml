name: Ping Test

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: [self-hosted, X64, Linux, terramaster, nas, personal, pythonproject1]
    steps:
      - name: Say hi
        run: echo "This job ran on a self-hosted Windows runner!"
